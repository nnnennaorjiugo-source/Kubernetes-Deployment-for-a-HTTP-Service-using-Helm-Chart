apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: httpbin-network-policy
spec:
  podSelector:
    matchLabels:
      app: {{ .Values.appName }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - protocol: TCP
          port: 80
  egress:
    - {}